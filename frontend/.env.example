# Target for the Vite dev-server proxy (all /api/* requests).
# CloudFront is stable across backend redeploys. Leave blank to use
# http://127.0.0.1:3000 (for `sam local start-api`).
VITE_API_BASE=https://asdfasdfasdf.cloudfront.net
